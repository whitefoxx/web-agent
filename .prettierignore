dist
node_modules
package-lock.json

# The submodules are separate repositories with their own formatting and their
# own `npm run check`. Formatting them from here would show up as a dirty
# submodule nobody asked for.
web-tools
marketplace

# Adapter files imported from xiaohongshu-operator — kept byte-identical with upstream
# so re-sync is just a cp. Don't reformat them here.
src/tools/**/*.js

# Captured DeepSeek HTML samples used as connector-selector references.
deepseek-*.html

# Store copy. `store/web-agent/store-listing.md` is pasted into the Chrome Web
# Store dashboard field by field: the exact spacing inside a line IS the
# deliverable, and Prettier re-wraps paragraphs that must stay on one line
# (see web-tools/scripts/check-store-listing.mjs). The rest of store/ is
# generated by render.mjs / raster.mjs.
store/*/store-listing.md
store/*/promo.html
store/*/svg/
store/*/images/
